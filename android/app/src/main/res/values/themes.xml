<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Browse UI uses the Leanback theme (provides TV styling + focus). -->
    <style name="AppTheme" parent="Theme.Leanback" />

    <!-- Fullscreen, no title bar, for video playback. -->
    <style name="PlaybackTheme" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen" />
</resources>
